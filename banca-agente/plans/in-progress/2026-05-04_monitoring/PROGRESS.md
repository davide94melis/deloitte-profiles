# Progresso Implementazione Modulo Monitoraggio (BR V6)

Data creazione: `2026-05-05`
Ultimo aggiornamento: `2026-05-21 09:00` (T-024 al 70%: Fasi A+B+C tecniche complete, 579/579 test FE monitoring PASS + 106/106 BE PASS. Restano D+E con funzionale ISP+Carmine)

## Riepilogo

| Metrica | Valore |
|---|---|
| Task totali | 31 |
| Completate | 29 |
| In corso | 2 |
| Da iniziare | 0 |
| Bloccate | 0 |
| Progresso complessivo | 100% tecnico, 70% T-024 (resta UAT funzionale + review finale) |

## Stato Task

| ID | Attività | Owner | Progresso | Stato | Branch | Note |
|---|---|---|---:|---|---|---|
| T-001 | Enum + Entità monitoring core | Alexios | 100% | Completata | feature/monitoring-structures | 3 enum + 2 entità + 1 migration SQL |
| T-002 | Entità Spread + MailingList + MonitoringDocument | Ahmad | 100% | Completata | feature/monitoring-entities-spread-mailing | 4 entità + 1 migration SQL |
| T-003 | DTO + Repository + Vista SQL estesa | Alexios | 100% | Completata | feature/monitoring-dto-repository-view | 7 DTO, 6 repository, 1 view entity, SQL view estesa. Build OK |
| T-004 | Modulo Angular monitoring + scaffold | Georgios | 100% | Completata | feature/monitoring-fe-scaffold | Scaffold FE completo: routing, models, enum, services, sidebar |
| T-005 | i18n labels + componenti condivisi monitoring | Adham | 100% | Completata | feature/monitoring | i18n IT/EN + semaforo 6 colori su branch principale FE |
| T-006 | Macchina stati 4 livelli + generatore ID + scadenziere | Alexios | 100% | Completata | feature/monitoring-state-machine | 3 service: MonitoringStateService, MonitoringIdGeneratorService, MonitoringScheduleGeneratorService. Build OK, pushed. |
| T-007 | Service + Controller pratiche monitoring | Davide | 100% | Completata | feature/monitoring-practice-service | Service, controller, repository, filter DTO, Excel export, createFromPostClosing trigger, terminatePractice con stato CONCLUSA, 6 PSM states monitoring. Build OK. |
| T-008 | Service + Controller covenant + eventi | Ahmad | 100% | Completata | feature/monitoring-covenant-events-service | 3 file: CovenantController, CovenantService, EventService. 372 righe. |
| T-009 | Dashboard monitoring BE endpoints | Ahmad | 100% | Completata | feature/monitoring-dashboard-be | 4 file: DashboardController, DashboardService, DashboardSummaryDTO, DashboardEventDTO |
| T-010 | Dashboard monitoring FE | Carmine | 95% | Da revieware | feature/monitoring-dashboard-fe | Audit 2026-05-19: tutti i requisiti implementati (donut 6 segmenti, polling 60s, 3 tabelle 13/13/12 col). Autore reale: Georgios. Mancano test unitari. |
| T-011 | Tabella pratiche monitoring FE | Georgios | 100% | Completata | feature/monitoring-practices-table-fe | 4 file FE + 2 i18n. Build OK. |
| T-012 | Dettaglio pratica ISP — header + tab scaffold | Adham | 100% | Completata | feature/monitoring-practice-detail-isp | Header, 3 tab, covenant cards, 3 accordion, 31 test. Build OK. |
| T-013 | Scadenziere unificato + dettaglio covenant singolo | Georgios | 100% | Completata | feature/monitoring-schedule-covenant-detail | BE: schedule endpoint + DTO esteso. FE: schedule-tab + covenant-detail dialog. Build OK. |
| T-014 | Upload Compliance Certificate + storico documenti | Alexios | 100% | Completata | feature/monitoring-upload-cc-documents | BE: service+controller+migration. FE: upload+history components, i18n. Build OK. |
| T-015 | Spread BE — service + controller (2 tabelle distinte) | Ahmad | 100% | Completata | feature/monitoring-spread-be | SpreadService (7 methods), SpreadController (7 endpoints), 14 unit test. |
| T-016 | Spread FE — 2 tabelle distinte (ISP + Deloitte) | Georgios | 100% | Completata | feature/monitoring-spread-fe | SpreadTabComponent: 2 tabelle, inline edit, CRUD, validazione, role-gating, 20 unit test. |
| T-017 | Flusso Deloitte — conferma/rifiuta + assegnazione CC manuale | Alexios (+ Davide test recovery) | 100% | Completata | feature/monitoring-deloitte-flow | 2026-05-20 (Davide): aggiunti cc-assignment.component.spec.ts (40 test su wizard 4-step, form validation, cascade assignEventValue→confirmDocument, edge case) e document-history.component.spec.ts (24 test su load via service, role-gating isConsultant, confirmDocument/rejectDocument, reject dialog con motivazione). Pattern NO_ERRORS_SCHEMA + MockCustomTranslatePipe. Totale 64/64 verdi. Wizard 4 step (vs 6 spec) → da validare con funzionale, non bloccante. Commit 53885b4. |
| T-018 | Flusso Deloitte — GenAI + modifica dati covenant | Davide | 100% | Completata | feature/monitoring-deloitte-genai | 2026-05-20 (Davide): implementazione BE+FE completa. **BE**: nuova entity MonitoringGenAiExtraction (mp_monitoring_genai_extraction con FK a mp_monitoring_document + status enum 5 valori + extractedPayload JSON LONGTEXT + audit), repository custom (3 finder), 2 DTO (MonitoringGenAiExtractionDTO + MonitoringGenAiApplyRequestDTO con inner CovenantUpdate/EventUpdate), migration 005_monitoring_genai.sql, GenAiDocumentType.COMPLIANCE_CERTIFICATE_MONITORING, 2 nuovi metodi state-guarded su MonitoringDocumentService (markGenAiInProgress, markGenAiToValidate), nuovo metodo updateDates su MonitoringEventService, MonitoringGenAiService (215 righe — startExtraction/getExtractionResult/applyExtraction con merge-patch covenant non-null), AsyncMonitoringGenAiProcess @Async @Transactional mock (LOG_PREFIX [T-018-MOCK], 9 chiavi COVENANT_NUM_* mock), MonitoringGenAiController (3 endpoint /api/v1/monitoring/documents/{id}/genai/* → 202/200/200). **FE**: enum MonitoringGenAiExtractionStatusEnum, 4 model interface (extraction + covenant update + event update + apply request), 3 metodi su monitoring.service (startGenAiExtraction, getGenAiResult, applyGenAiExtraction via restService pattern), MonitoringGenAiExtractionComponent standalone (p-dialog + ReactiveForm dinamico su fieldKeys + StatusDialogService warning before apply), integrazione document-history (4 props stato + 5 metodi pubblici + 2 button role-gated Consultant: pi-bolt "Estrai" su IN_ATTESA_CONFERMA/VERIFICA_IN_CORSO, pi-pencil "Modifica Dati" su GENAI_DA_VALIDARE), MonitoringGenAiExtractionModule registrato in MonitoringPracticeDetailModule, i18n IT+EN sezione monitoring.genai.* (title/subtitle/actions/fields/messages). **Decisioni**: (a) riuso useCase POST_CLOSING + nuovo documentType COMPLIANCE_CERTIFICATE_MONITORING (no coord. con team AI esterno), (b) @Async attivato (AsyncConfig esistente con asyncThreadPoolExecutor), (c) mock locale per stub AI (la produzione sostituira' con AIManagerService.startAIProcess), (d) mapping CovenantUpdate via merge/patch (legge stato corrente, sovrascrive solo campi non-null), (e) form 2-colonne unico (semplificazione del pattern GenaiExtractionComponent originale — sufficient per CC). **Merge feature/monitoring** (commit 853ca3f BE + 6a4e6da FE) per ereditare fix test practice-detail dal branch principale. **Fix collaterali specs** post-merge (commit 18ee0e3 FE): spread-tab/practices/schedule-tab/practice-detail allineati alle component refactorizzate (4 file, 66 ins / 83 del). **Verifiche**: Fase A BE (mvn clean compile SUCCESS, 17 service + 6 controller + 37 regression event = 60/60 PASS), Fase A FE (npx tsc clean, monitoring-genai-extraction 26/26 PASS, document-history 30/30 PASS, practice-detail 38/38 PASS post-merge); Fase B coerenza (17/19 requisiti completi, 2 semplificazioni documentate per UAT: pattern dialog semplificato vs DynamicDialog originale, form 2-colonne unico vs dialog separato covenant); Fase C riesame (codice 215 righe MonitoringGenAiService entro limiti CONST, constructor injection, structured logging no PII, Optional pattern corretto, layered separation rispettata). Commit pushati: BE feature/monitoring-deloitte-genai (d91065e foundation+orchestrator + 853ca3f merge), FE feature/monitoring-deloitte-genai (foundation+component + 6a4e6da merge + 18ee0e3 fix collaterali). |
| T-019 | Eccezioni ISP + gestione Deloitte | Adham (+ Davide recovery) | 100% | Completata | feature/monitoring-exceptions | 2026-05-20 (Davide): merge feature/monitoring in branch (risolti conflitti su MonitoringEventService.java import LocalDateTime/ChronoUnit, MonitoringEventServiceTest.java unione 44 test). FE wiring: import MonitoringExceptionDialogModule in practice-detail.module, wire `<app-monitoring-exception-dialog>` con @ViewChild, action column buildActionCell role-gated (ISP: Salta/Ignora/Visualizza; Deloitte: Annulla), monitoring.service.requestException → FormData multipart, getExceptionDocument GET Blob. Fix test preesistenti exception-dialog.spec.ts (ButtonModule + CUSTOM_ELEMENTS_SCHEMA + stub post-detectChanges). Risultato: 27 test exception-dialog + 32 test practice-detail + 5 test monitoring.service = 64/64 verdi. BE: build SUCCESS, test MonitoringEventServiceTest 44 verdi. Branch da pushare. |
| T-020 | Upload COVNO/DB Obblighi + card dashboard | Alexios (+ Davide test recovery) | 100% | Completata | feature/monitoring-covno-upload | 2026-05-20 (Davide): aggiunti CovnoUploadServiceTest.java (20 test in 5 @Nested: ParseXlsx, MergePrecedence A-011 verificato, HistoryPersistence SUCCESS/PARTIAL/FAILED, GetHistory, EdgeCases) e CovnoControllerTest.java (5 test pure unit) — totale 25 BE verdi, BUILD SUCCESS. Aggiunto dashboard.component.spec.ts FE (14 test su upload card, ngOnInit reload, getResultSeverity mapping completo) — 14/14 verdi. Commit BE d5d7272, FE c24ffac. |
| T-021 | Pagine complete Scaduti + In Scadenza | Adham (+ Davide recovery) | 100% | Completata | feature/monitoring-expired-expiring-pages | 2026-05-20 (Davide): audit cross-branch ha verificato 20/22 requisiti OK (2 pagine, 13 colonne, filtri colonna + range date, app-table server-paginated 20 righe, export Excel, route lazy, 62+62 unit test, i18n IT+EN). Gap: link "Vedi tutte" della dashboard puntavano a navigateToPractices() invece che alle nuove pagine. Recovery: merge T-010 (feature/monitoring-dashboard-fe) nel branch T-021, aggiunti navigateToExpiredEvents()/navigateToExpiringEvents() in dashboard.component.ts, patchati i 2 click handler in dashboard.component.html, aggiunti 2 unit test AAA in dashboard.component.spec.ts. Test totali verdi: 79 dashboard + 62 expired + 62 expiring = 203/203. Commit fix 9f5fdcc. |
| T-022 | Mailing List completo | Georgios | 100% | Completata | feature/monitoring-mailing-list | BE: MailingListService, Controller, AdForm entity+migration, 31 test. FE: main page+detail, i18n. Build OK. |
| T-023 | Email templates + scheduled jobs | Davide | 100% | Completata | feature/monitoring-notifications-jobs | 7 enum email, MonitoringSchedulerService, 4 @Scheduled jobs, 7 template HTML (EM). Build OK. |
| T-024 | Integration testing + UAT + polish | Davide | 70% | In corso | feature/monitoring-integration-uat | 2026-05-20+21: Fasi A+B+C tecniche complete. **Fase A** (fix test residui): practices.spec 2 FAIL → 47/47 PASS (riscritti 2 test allineati a FormGroup-driven filters vs deprecated LazyLoadEvent.filters + 2 bonus alignment), spread-tab.spec 3 xit aggiornati a "T-024 verified: feature removed, no replacement needed", mailing-list-detail.spec 45 FAIL + 6 xit → 50/50 PASS (fixato NullInjector HttpClient via HttpClientTestingModule + UserService mock + riscritti 5 xit per inline editing API addContactRow/startEditRow/saveContactRow). **Fase B** (build+test totali): BE mvn clean install SUCCESS + 106/106 monitoring test PASS; FE npx tsc CLEAN + suite monitoring 579/579 PASS dopo fix collaterali aggiuntivi su schedule-tab.spec (31 FAIL → 31 PASS via HttpClientTestingModule + AuthService mock + thead 9 col realign), covenant-detail.spec (5 FAIL → 23 PASS via thead 11 col realign + sign-only row mapping), mailing-list.spec (17 FAIL → 17 PASS via same TestBed pattern + 11/12 col headers). Pre Fase B: 526 PASS / 53 FAIL / 3 SKIP. Post Fase B: 579 PASS / 0 FAIL / 3 SKIP intentional. **Fase C** (cleanup): BE working tree pulito, no debug statements nei file T-018, plans/ in BE è strutturale non residuo. Commit `a1fefb9` (6 spec realign), branch BE+FE pushati. **Resta D+E** (sessione successiva con stakeholder): D=UAT funzionale E2E ISP+Deloitte (richiede utente reale ISP e mock data), E=code review finale Davide BE + Carmine FE. |
| T-MERGE-001-002 | Merge entità fondazioni in feature/monitoring | Alexios | 100% | Completata | feature/monitoring-dto-repository-view | Mergiati T-001 e T-002 nel branch T-003 |
| T-MERGE-003 | Merge fondazioni BE | Alexios | 100% | Completata | feature/monitoring | git pull origin feature/monitoring-dto-repository-view in feature/monitoring. Build OK. |
| T-MERGE-004 | Merge FE scaffold | Georgios | 100% | Completata | feature/monitoring | Fast-forward merge, build OK. Pushed 2026-05-06 |
| T-MERGE-006 | Merge core BE parziale (macchina stati + pratiche) | Davide | 100% | Completata | feature/monitoring | Fast-forward merge T-006 + T-007 in feature/monitoring. Build OK. T-008 sbloccata. |
| T-MERGE-008 | Merge core BE | Davide | 100% | Completata | feature/monitoring | Fast-forward merge T-008 in feature/monitoring. Build SUCCESS. |
| T-MERGE-FINAL | Merge finale pre-integrazione | Davide | 100% | Completata | feature/monitoring | 2026-05-20: 13 merge ordinati eseguiti in feature/monitoring (BE 7 + FE 8) via sequence dashboard → exceptions → covno → genai → expired/expiring → mailing-list → notifications-jobs. Conflitti risolti: BE (migration 005 collision risolta rinominando 005_genai → 006_genai; MonitoringEventRepository combinazione metodi; PROGRESSO_BR residuo via --ours), FE (T-019: 4 file via sub-agent; T-020: 3 file dashboard via sub-agent; T-018: 3 file practice-detail via sub-agent; T-021: 3 file i18n+service via sub-agent; T-022 mailing auto-merge). Fix collaterali specs post-merge: practice-detail.spec (mock mancante getExpiredEventsByPractice → 32/32 PASS), mailing-list-detail.spec (5 xit + 5 type-guard fix per StatusDialogModel.button optional). Verifiche: BE mvn clean compile SUCCESS + 87/87 test critici PASS (MonitoringGenAi + MonitoringEventService + CovnoUpload); FE npx tsc CLEAN, dashboard 92/92 PASS, monitoring-genai-extraction 26/26 PASS, practice-detail 32/32 PASS. Pushed: BE feature/monitoring (3b8e9b5..0883e59), FE feature/monitoring (95dffb3..0dc71e2). Sblocca T-024. |
| T-MERGE-W2 | Merge checkpoint Wave 2 | Davide | 100% | Completata | feature/monitoring | Mergiati 3 branch BE + 5 branch FE. Conflitti i18n e practice-detail risolti. Build BE+FE: SUCCESS. |
## Log Attività

### 2026-05-05
- Progresso file creato
- T-001 completata (Alexios): creati 3 enum (MonitoringPracticeStateEnum, MonitoringEventStateEnum, MonitoringDocumentStateEnum), 2 entità JPA (MonitoringCovenant, MonitoringEvent), 1 migration SQL (002_monitoring_foundation.sql). Build: SUCCESS. Branch: feature/monitoring-structures
- T-002 completata (Ahmad): create 4 entità (SpreadTable, SpreadHistory, MailingListContact, MonitoringDocument), 1 migration SQL (003_monitoring_spread_mailing.sql). Branch: feature/monitoring-entities-spread-mailing
- T-004 completata (Georgios): modulo Angular monitoring scaffold con routing (admin + consultant + back-office/ISP), 4 sotto-pagine, 8 modelli, 4 enum, 3 servizi, sidebar menu item + i18n IT/EN. Build OK. Branch: feature/monitoring-fe-scaffold
- T-005 completata (Adham): chiavi i18n monitoring in it-IT.json e en-GB.json, componente semaforo con 6 colori distinti (OK→verde, InScadenza→giallo, DaAttenzionare→arancione, Scaduto→rosso, FuoriSoglia→rosso scuro, Conclusa→grigio). Branch: feature/monitoring (diretto)

### 2026-05-06
- Aggregazione cross-branch: verificati 4 branch remoti (monitoring-structures, monitoring-entities-spread-mailing, monitoring-fe-scaffold, monitoring). T-001 e T-002 verificate su BE repo. T-004 e T-005 verificate da PROGRESSO + conferma TL.
- Aggiunti 4 merge checkpoint espliciti al piano: T-MERGE-001-002, T-MERGE-006, T-MERGE-W2, T-MERGE-FINAL. Motivo: task con dipendenze su branch separati non potevano partire (T-003 bloccata, T-008 bloccata, T-017 bloccata).
- T-MERGE-004 completata (Georgios): fast-forward merge di feature/monitoring-fe-scaffold in feature/monitoring (FE). 50 file, +1080 righe, nessun conflitto. Build production: SUCCESS. Pushed su origin/feature/monitoring. Wave 0 al 71% (5/7 task completate).
- **T-MERGE-001-002 completata (Alexios)**: mergiati branch T-001 (feature/monitoring-structures) e T-002 (origin/feature/monitoring-entities-spread-mailing) nel branch feature/monitoring-dto-repository-view. Conflitto risolto su MonitoringDocumentStateEnum (kept T-001 version with text descriptions).
- **T-003 completata (Alexios)**: Branch `feature/monitoring-dto-repository-view` creato da `feature/monitoring`, mergiati T-001 e T-002 nel branch. Creati: 7 DTO (MonitoringCovenantDTO, MonitoringEventDTO, SpreadTableDTO, SpreadHistoryDTO, MailingListContactDTO, MonitoringDocumentDTO, MonitoringPracticeSummaryDTO), 6 repository con JpaSpecificationExecutor (MonitoringCovenantRepository, MonitoringEventRepository, SpreadTableRepository, SpreadHistoryRepository, MailingListContactRepository, MonitoringDocumentRepository), 1 view entity (MonitoringPracticeSummaryView), SQL view estesa con deal_name, user names, expired/expiring/docs counts. Build: SUCCESS.
- Wave 0: 7/31 task completate (23%). Prossimo step: T-MERGE-003 (merge fondazioni BE) sbloccata.

### 2026-05-07
- **T-MERGE-003 completata (Alexios)**: `git pull origin feature/monitoring-dto-repository-view` su feature/monitoring. T-001 + T-002 + T-003 ora in feature/monitoring. T-006 e T-007 sbloccate.
- Wave 0 completata. 8/31 task completate (26%). Prossimo step: T-006 (macchina stati, Alexios) e T-007 (pratiche service, Davide) ora sbloccate.
- **T-006 completata (Alexios)**: Branch `feature/monitoring-state-machine`. Creati 3 service: MonitoringStateService, MonitoringIdGeneratorService, MonitoringScheduleGeneratorService. Build: SUCCESS.
- **T-007 completata (Davide)**: 5 file + createFromPostClosing + terminatePractice + trigger auto-creazione + 6 PSM states. Build: SUCCESS.
- **T-MERGE-006 completata (Davide)**: fast-forward merge T-006 + T-007 in feature/monitoring. Build: SUCCESS. T-008 sbloccata.

### 2026-05-19
- **Audit cross-branch (Davide, BE Senior)**: verifica stato reale di 4 task marcate come "Da iniziare" o "In corso" nel PROGRESS ma con commit presenti sui branch remoti. Risultati:
  - **T-010** (Dashboard FE) — passata da 0% Da iniziare a **95% Da revieware**. Tutti i requisiti del piano implementati (donut chart 6 segmenti con palette esatta, polling 60s pattern corretto, 3 tabelle con conteggio colonne 13/13/12). Autore reale del commit: Georgios (non Carmine). Manca solo test unitario (`dashboard.component.spec.ts`).
  - **T-017** (Deloitte flow) — riassesata da 90% In corso a **85% In corso**. Implementazione funzionalmente corretta (role-gating + CC assignment dialog + FormGroup reattivo) ma wizard a 4 step invece dei 6 step espliciti previsti dal piano (Step 4/5/6 collassati in unico "Assegna"). Mancano test (`cc-assignment.component.spec.ts`, `document-history.component.spec.ts`).
  - **T-019** (Eccezioni) — passata da 0% Da iniziare a **50% In corso**. BE 95% (7 test passanti, branch BE behind di feature/monitoring → serve rebase). FE 30%: il dialog `exception-dialog.component` esiste con 33 test passanti ma è completamente DISCONNESSO dal flusso utente — `MonitoringExceptionDialogModule` non importato in `MonitoringPracticeDetailModule`, selettore mai usato in template parent, action column ha label statica senza click handler, e `monitoring.service.requestException` invia JSON invece di multipart (415/400 a runtime). **BLOCKER per T-MERGE-FINAL**.
  - **T-020** (Covno upload) — passata da 0% Da iniziare a **85% Da revieware**. Implementazione BE (CovnoUploadService con parseXlsx Apache POI, mergeData con priorità A-011, CovnoUploadHistory entity, migration 005, MonitoringDataSourceEnum) + FE (card dashboard COVNO, dialog upload, dialog storico, i18n IT/EN 22 chiavi) completa. Autore: Alexios. Mancano test BE (CovnoUploadServiceTest) e FE.
- **Disallineamento PROGRESS rilevato**: alcuni sviluppatori (Adham, Alexios, Carmine→Georgios) hanno pushato codice senza aggiornare il PROGRESS.md. Procedura "Aggiornamento del progresso" della skill sdlc-executor da rinforzare.
- **Conseguenza per T-MERGE-FINAL**: bloccata da T-019 (FE wiring mancante). T-MERGE-FINAL può partire solo dopo fix integrazione FE eccezioni.
- **Sblocchi**: T-018 (GenAI) tecnicamente sbloccata (T-017 sostanzialmente completa, gap solo su test/UX).

### 2026-05-20
- **T-019 completata (Davide, recovery FE wiring)**: durata sessione ~2h. Risolto BLOCKER T-MERGE-FINAL.
  - **BE merge**: branch `feature/monitoring-exceptions` rebased contro `origin/feature/monitoring`. Conflitti risolti su `MonitoringEventService.java` (import LocalDateTime+ChronoUnit insieme; combinazione metodi `getExceptionDocuments` con `findScheduleByPractice`) e `MonitoringEventServiceTest.java` (sub-agent generale: unione 44 test totali da entrambi i branch, mock MonitoringDocumentRepository + stato @InjectMocks 3-deps; adattati 3 test origin a nuova signature 3-param requestException).
  - **FE wiring** (sub-agent generale, ~30min): aggiunto import `MonitoringExceptionDialogModule` in `practice-detail.module.ts`, renderizzato `<app-monitoring-exception-dialog #exceptionDialog>` in template con `@ViewChild`, refattorizzato `buildActionCell()` con `DynamicComponentModel<ButtonComponent>` role-gated (ISP: Salta/Ignora/Visualizza; Deloitte: Annulla; nessuna action per Deloitte su evento senza eccezione), aggiunto `getExceptionDocument` su service. Fix critico `monitoring.service.requestException`: ora costruisce `FormData` con `motivation` + `file?` opzionale e invia con `httpClient.post` (`RestService` non supporta override Content-Type per multipart).
  - **Fix test preesistenti exception-dialog.spec.ts** (bug Adham mai eseguito): cambiato `NO_ERRORS_SCHEMA → CUSTOM_ELEMENTS_SCHEMA`, importato `ButtonModule` per consentire al template di renderizzare `<app-button [style]="buttonEnum.*">` correttamente, spostata assegnazione `dialogStub` DOPO `fixture.detectChanges()` per evitare override da ViewChild.
  - **Risultato verifiche**:
    - Fase A (tecnica): BE `mvn clean compile`: SUCCESS. BE `mvn test -Dtest=MonitoringEventServiceTest`: PASS. FE `ng test`: 64/64 verdi (27 exception-dialog + 32 practice-detail + 5 monitoring.service).
    - Fase B (coerenza): tutti 11 requisiti T-019 (5 FE + 3 BE + 3 service) verificati con file:linea citato.
    - Fase C (riesame): codice nei limiti progetto (<800 righe/file, <50/metodo), AAA pattern test, role-gating coerente, nessun magic number.
  - **File modificati BE** (18): merge da feature/monitoring + risoluzione conflitti su 2 file. Tutti i nuovi entry sono allineamenti.
  - **File modificati FE** (6): `practice-detail.{component.ts, component.html, component.spec.ts, module.ts}` + `monitoring.service.ts` + `exception-dialog.component.spec.ts`. Aggiunto `monitoring.service.spec.ts` (nuovo, 5 test).
  - **Pronto per commit**: branch BE `feature/monitoring-exceptions` (merge commit), branch FE `feature/monitoring-exceptions` (5 file modificati + 1 nuovo). Sviluppatore deve eseguire commit+push.
  - **T-MERGE-FINAL** ora sbloccata. T-MERGE-FINAL dipende ancora dal commit+push dei branch T-017 e T-020 (entrambi al ~85% per gap test).
- **T-017 chiusa al 100% (Davide test recovery)**: aggiunto cc-assignment.component.spec.ts (40 test) + document-history.component.spec.ts (24 test). Tutti i wizard step coperti, role-gating, edge case. 64/64 verdi. Commit FE 53885b4. Wizard 4 step vs 6 spec resta come decisione UX da validare con funzionale (non bloccante).
- **T-020 chiusa al 100% (Davide test recovery)**: BE — CovnoUploadServiceTest.java 20 test in 5 @Nested incluso verifica esplicita A-011 (AUTO sovrascritto, COVNO aggiornato, MANUAL_DELOITTE intatto) + CovnoControllerTest.java 5 test (BUILD SUCCESS, 25 tests). FE — dashboard.component.spec.ts 14 test (14/14 verdi). Commit BE d5d7272, FE c24ffac.
- **T-MERGE-FINAL ora effettivamente sbloccato**: tutte le task Wave 3+4 (T-017..T-023) sono al 100%. Davide può procedere con merge di tutti i branch in feature/monitoring.
- **T-021 chiusa al 100% (Davide recovery)**: audit cross-branch su `feature/monitoring-expired-expiring-pages` ha confermato che Adham ha implementato 20/22 requisiti del piano (2 pagine expired/expiring con 13 colonne identiche al `MonitoringDashboardEventModel`, filtri colonna + range date `p-calendar`, `app-table` server-paginated 20 righe, export Excel via blob + `Report_*.xlsx`, route lazy in `monitoring-routing.module.ts`, 62+62 unit test, i18n IT+EN). Gap bloccante: i link "Vedi tutte" nella dashboard puntavano a `navigateToPractices()` invece che alle nuove pagine. Recovery: (1) merge `feature/monitoring-dashboard-fe` (T-010) nel branch T-021 — merge auto strategy "ort" senza conflitti, commit `6805222`. (2) Aggiunti `navigateToExpiredEvents()` e `navigateToExpiringEvents()` in `dashboard.component.ts` simmetrici a `navigateToPractices()` (`['../expired-events']` / `['../expiring-events']` relativeTo route). (3) Patchati 2 click handler in `dashboard.component.html` (terzo link "Vedi tutte" su Pratiche Recenti lasciato su `navigateToPractices()`, corretto). (4) Aggiunti 2 unit test AAA in `dashboard.component.spec.ts`. Verifiche: Fase A — 79 test dashboard (77+2) + 62 expired + 62 expiring = 203/203 verdi. Fase B — i 3 requisiti #17, #18, #15+#16 (route già esistenti) verificati con file:linea. Fase C — codice nei limiti CONST (max 50 righe/funzione, no magic numbers, AAA). Commit fix `9f5fdcc`, branch pushato.
- **Conseguenza**: T-MERGE-FINAL ora bloccato solo da T-018 (mia). Posso partire con T-018 (Flusso Deloitte — GenAI + modifica covenant).
- **T-018 chiusa al 100% (Davide)**: implementazione BE+FE completa del Flusso Deloitte GenAI per documenti monitoring. Durata sessione ~5h.
  - **Architettura**: (a) riuso `useCase POST_CLOSING` + nuovo `documentType COMPLIANCE_CERTIFICATE_MONITORING` in `GenAiDocumentType`, (b) nuova entity `MonitoringGenAiExtraction` (mp_monitoring_genai_extraction) per persistenza risultato GenAI, (c) `@Async` attivato (verificato `AsyncConfig` con `asyncThreadPoolExecutor` esistente), (d) mock locale (`AsyncMonitoringGenAiProcess` con `LOG_PREFIX [T-018-MOCK]` + 9 chiavi `COVENANT_NUM_*` simulate) — la produzione sostituira' con `AIManagerService.startAIProcess` quando il bridge AI esterno sara' configurato.
  - **BE creati** (5 file production + 2 test + migration): `MonitoringGenAiExtraction`, `MonitoringGenAiExtractionStatusEnum`, `MonitoringGenAiExtractionRepository`, `MonitoringGenAiExtractionDTO`, `MonitoringGenAiApplyRequestDTO` (inner static CovenantUpdate/EventUpdate), `MonitoringGenAiService` (215 righe — startExtraction/getExtractionResult/applyExtraction con merge-patch covenant non-null), `AsyncMonitoringGenAiProcess` (134 righe — @Async @Transactional mock con state transitions PENDING→IN_PROGRESS→READY_FOR_VALIDATION+markGenAiToValidate o FAILED), `MonitoringGenAiController` (77 righe — 3 endpoint /api/v1/monitoring/documents/{id}/genai/* con 202/200/200), migration `005_monitoring_genai.sql`.
  - **BE modificati**: `GenAiDocumentType.COMPLIANCE_CERTIFICATE_MONITORING`, `MonitoringDocumentService.markGenAiInProgress` + `markGenAiToValidate` (state-guarded), `MonitoringEventService.updateDates(eventId, refDate, expDate)` per supportare update sole date dall'estrazione GenAI.
  - **BE test**: `MonitoringGenAiServiceTest` 17 test in 4 @Nested (StartExtraction, GetExtractionResult, ApplyExtraction, ToDtoEdgeCases) + `MonitoringGenAiControllerTest` 6 pure unit (202/200/200 + auth propagation + null Authentication fallback). 60/60 PASS (23 nuovi + 37 regression `MonitoringEventServiceTest`).
  - **FE creati** (5 file): `monitoring-genai-extraction-status.enum.ts`, `monitoring-genai-extraction.model.ts` (4 interface), `monitoring-genai-extraction.component.{ts,html,scss,module,spec}` (235 + 60 + 7 + 24 + 461 righe — dialog p-dialog [(visible)] con ReactiveForm dinamico su fieldKeys + SpinnerComponent + StatusDialogService warning prima dell'apply).
  - **FE modificati**: `monitoring.service.ts` (3 metodi `startGenAiExtraction` / `getGenAiResult` / `applyGenAiExtraction` via restService pattern), `document-history.component.{ts,html,spec}` (+4 props stato + 5 metodi pubblici + 2 button role-gated `appHasRole=Consultant`: `pi-bolt` "Estrai con GenAI" su `IN_ATTESA_CONFERMA|VERIFICA_IN_CORSO`, `pi-pencil` "Modifica Dati" su `GENAI_DA_VALIDARE`; +6 test), `practice-detail.module.ts` (importato `MonitoringGenAiExtractionModule`), i18n IT+EN sezione `monitoring.genai.*` (title/subtitle/extractAction/modifyAction/confirmAction/validateAction/cancelAction/loadingExtraction/extractionFailed/noExtractionAvailable/fields.{covenantType,valueLimit,sign,periodicity,variable,notes,referenceDate,expirationDate,actualDate,monitoringValue}/covenantSection/eventSection/applySuccess/applyError/extractionStartedSuccess/validateConfirmTitle/validateConfirmMessage).
  - **Merge `feature/monitoring` nel branch T-018**: dopo i primi commit T-018 il sub-agent ha verificato che `practice-detail.component.spec.ts` falliva sul branch base T-017 (`feature/monitoring-deloitte-flow`) — 38/38 FAILED preesistente. Mergiando `feature/monitoring` dentro `feature/monitoring-deloitte-genai` (BE commit `853ca3f`, FE merge auto con 587 righe modificate) i test practice-detail tornano 38/38 PASS. Questo merge riduce anche i conflitti per T-MERGE-FINAL.
  - **Fix collaterali specs post-merge** (FE commit `18ee0e3`): 4 file spec stale rispetto a componenti refactorizzate post-merge (spread-tab `SpreadTableModel` shape, practices `onSemaphoreFilterChange → applyFilters`, schedule-tab `onCovenantTypeChange → applyFilters`, practice-detail `CustomTranslatePipeMock` + `getEventsByCovenant` stub). Risultato: spread-tab 40 PASS + 3 xit (downloadDocument rimosso dal component), practices 45 PASS / 2 FAIL preesistenti runtime non TS (filtri ora gestiti da `FormGroup` invece di `LazyLoadEvent.filters`, fuori scope T-018), schedule-tab PASS, practice-detail 38/38 PASS. Nessuna modifica al codice prod.
  - **Verifiche complete**: Fase A — BE `mvn clean compile` SUCCESS + 60/60 test PASS; FE `npx tsc --noEmit` SUCCESS + monitoring-genai-extraction 26/26 + document-history 30/30 + practice-detail 38/38. Fase B — 17/19 requisiti del piano implementati e verificati con file:linea+test; 2 semplificazioni documentate (#15 dialog pattern p-dialog [(visible)] vs DynamicDialog originale, #16 form 2-colonne unico vs dialog separato covenant — accettabili per scope MVP, ampliabili in iterazione successiva). Fase C — riesame critico passato: codice 215 righe servizio entro CONST (max 800 file, max 50 metodo, no magic numbers, structured logging no PII, AAA test, layered separation, constructor injection, Optional pattern coerente).
  - **Commit + push**: BE `feature/monitoring-deloitte-genai` (commit `d91065e` foundation+service+controller+test + `853ca3f` merge feature/monitoring), FE `feature/monitoring-deloitte-genai` (commit `61ef60e` foundation+service+model+i18n+integrazione+test + `6a4e6da` merge feature/monitoring + `18ee0e3` fix collaterali specs).
  - **Semplificazioni segnalate per UAT funzionale**: (a) il dialog "Modifica Dati" usa form 2-colonne con tutti i field estratti modificabili in un unico passaggio (no wizard separato covenant vs event come nel BR originale) — `buildPayload` separa logicamente i campi in covenantUpdates e eventUpdates al submit; (b) no preview PDF integrato (`PdfViewerWrap`) come nel `GenaiExtractionComponent` pre-closing — i field estratti sono mostrati direttamente in form. Entrambe sono coerenti col principio "no features beyond what task requires" e con la natura mock del flusso GenAI.
- **T-MERGE-FINAL ora completamente sbloccata**: tutte le task Wave 3+4 (T-017..T-023) sono al 100%. Posso procedere al merge di tutti i branch in `feature/monitoring`. Nota di efficienza: la maggior parte e' gia' inclusa via merge intermedi (T-018 ha mergiato feature/monitoring, T-019 idem). Restano da mergiare i branch T-017 / T-018 / T-019 / T-020 / T-021 (con il fix Vedi tutte) in `feature/monitoring`. Sblocca T-024 (Integration testing + UAT + polish).
- **T-MERGE-FINAL chiusa al 100% (Davide)**: 13 merge ordinati eseguiti su feature/monitoring (BE 7 branch + FE 8 branch). Durata sessione ~2.5h.
  - **BE merge sequence**: T-009 dashboard-be (272 ins) → T-017 deloitte-flow (already up to date) → T-019 exceptions (44 ins event service + 230 test) → T-020 covno-upload (1222 ins, NEW migration 005_covno collision) → **rename 005_genai → 006_genai** (commit `719380b`) → T-018 deloitte-genai (1318 ins) → T-022 mailing-list (PROGRESSO_BR conflict → --ours) → T-023 notifications-jobs (PROGRESSO_BR + MonitoringEventRepository conflict → combine methods).
  - **FE merge sequence**: T-010 dashboard-fe (1434 ins) → T-017 deloitte-flow (1354 ins: cc-assignment + 3 spec files) → T-019 exceptions (4 conflicts: practice-detail.{ts,html,spec,module} + monitoring.service — sub-agent risolto: tutti i ViewChild/proprieta'/metodi/import combinati di entrambi i branch, requestException multipart fix da T-019, ButtonModule aggiunto a TestBed) → T-020 covno-upload (3 conflicts: dashboard.module + environment.{ts,development.ts} — sub-agent risolto: ButtonModule alias PrimeButtonModule per evitare collision con shared button, monitoringEvents+monitoringCovno endpoints combinati in api.private; poi altri 4 conflitti su dashboard.{component.{ts,html,spec.ts},gitignore} risolti combinando: tenuto INTERO dashboard T-010 + AGGIUNTA card COVNO subito sotto donut, 7 proprieta' + 4 metodi COVNO, 79 test T-010 + 14 test T-020 = 93 totali) → T-018 deloitte-genai (3 conflicts: practice-detail.spec.ts + practice-detail.module.ts + monitoring.service.ts — sub-agent risolto: combinati metodi GenAI con metodi COVNO HEAD; MonitoringGenAiExtractionModule aggiunto a practice-detail.module insieme a MonitoringExceptionDialogModule; CustomTranslatePipeMock e getEventsByCovenant gia' presenti in HEAD; spec collaterali T-018 fix 18ee0e3 portati via merge) → T-021 expired-expiring-pages (3 conflicts: monitoring.service.ts + i18n.{it,en}.json — sub-agent risolto: aggiunti exportExpiredEventsExcel + exportExpiringEventsExcel; chiavi i18n monitoring.expiredEvents.* + expiringEvents.* combinate con genai.* esistenti; fix Vedi tutte gia' presente in HEAD) → T-022 mailing-list (auto-merge 2251 ins).
  - **Fix collaterali specs post-merge** (commit `0dc71e2`): practice-detail.spec post-merge mostrava 12 FAILED (was 38, post-merge 32/20PASS+12FAIL). Causa unica: spy `getExpiredEventsByPractice` dichiarato ma senza returnValue, `forkJoin({practice, covenants, expired, expiring})` esplode con "undefined stream", tutti i tbody/thead restano vuoti. Single-mock fix → 32/32 PASS. mailing-list-detail.spec: 10 errori TS preesistenti dal branch T-022 (5 metodi rimossi: `openAddContactDialog`/`openEditContactDialog`/`saveContact`/`showContactDialog`/`isEditMode` → inline editing `addContactRow`/`startEditRow`/`saveContactRow`/`cancelEditRow`; 5 `statusArg.button` possibly undefined → expect.toBeDefined + `!` assertion). 5 test marcati `xit()` con TODO T-MERGE-FINAL followup per riscrittura su inline editing API in T-024.
  - **Verifiche post-merge**: BE `./mvnw clean compile -DskipTests` SUCCESS. BE `./mvnw test -Dtest='MonitoringGenAi*Test,MonitoringEventServiceTest,CovnoUploadServiceTest'` 87/87 PASS. FE `npx tsc --noEmit --skipLibCheck` ZERO errori. FE `ng test` su dashboard.component.spec.ts 92/92 PASS, monitoring-genai-extraction.component.spec.ts 26/26 PASS, practice-detail.component.spec.ts 32/32 PASS post-fix. Fail residui preesistenti fuori scope T-MERGE-FINAL: practices.spec 2 FAIL (refactor FormGroup-driven, non LazyLoadEvent.filters), spread-tab 3 xit downloadDocument (feature rimossa), mailing-list-detail 5 xit inline editing (T-022 refactor) — tutti da risolvere in T-024 polish.
  - **Pushed**: BE feature/monitoring (3b8e9b5..0883e59), FE feature/monitoring (95dffb3..0dc71e2).
  - **T-024 ora sbloccata**: integration testing E2E + UAT funzionale + polish puo' iniziare. Suggerito per T-024: (a) verificare flussi end-to-end ISP+Deloitte, (b) fixare i 2 fail preesistenti practices.spec, (c) riscrivere i 5+3 xit con la nuova API, (d) testare GenAI mock fino al callback, (e) testare COVNO upload reale con XLSX di esempio.

### 2026-05-25
- **T-024 in corso (Davide, smoke E2E STEP 3-4)**: smoke end-to-end ripreso da handoff `HANDOFF_Smoke_E2E_T024_20260524.md`. Branch `feature/monitoring-integration-uat` su BE+FE. Durata sessione ~5h. Pratica fixture in DB: M1 (id 8288, MONITORING_OK).
  - **STEP 3 Dashboard Monitoraggio chiusa**. Fix 3 bug critici:
    1. **Crash JS `Cannot read properties of undefined (reading 'toString')` in `mapPracticeToRow`**: il DTO BE `MonitoringPracticeSummaryDTO` mandava `dealId`/`contractId`/`expiredCount`/`expiringCount`/`docsInProgressCount`/`semaphoreColor` mentre il FE `MonitoringPracticeModel` si aspettava `trackingId`/`contractKey`/`countExpired`/`countExpiring`/`countDocsInProgress`/`semaphoreState` + `endDate`/`clientNdg` totalmente assenti. Stesso bug colpiva `MonitoringPracticeService.findAll/findById`. Fix: rinominati i 6 campi del DTO, aggiunti `endDate` (da `mp.termination_date`) e `clientNdg`, `mapStateToSemaphore` sostituito con `stripMonitoringPrefix` che ritorna l'enum `OK`/`IN_SCADENZA`/... atteso dal `SemaphoreComponent`. View `vw_monitoring_practice_summary` aggiornata con 2 colonne `end_date`+`client_ndg`, entity allineata. Commit BE `888e704`.
    2. **6 stati `Monitoraggio *` comparivano nel widget SACE della dashboard principale `/user`**: la migration 008 aveva inserito i 6 stati MONITORING_* con `psm_phase_id=3` (SACE_DOMANDA_GARANZIA) perche' la phase MONITORING non esisteva ancora in `mp_psm_phase`. Migration `009_monitoring_phase_fix.sql` crea la phase MONITORING (codice gia' nell'ENUM) e sposta i 6 stati con UPDATE idempotente. Allineato anche `PsmPhaseCodeEnum.MonitoringReporting` FE da `'MONITORING_REPORTING'` → `'MONITORING'` per match col DB. Commit BE `888e704` + FE `bdc195a`.
    3. View DDL applicata manualmente in DB locale via mysqlsh (`vw_monitoring_practice_summary` + migration 009). Necessario riapplicare in dev/UAT/prod (no Flyway/Liquibase su view).
  - **STEP 4 Tabella Pratiche Monitoraggio in corso**. Sweep preventivo con sub-agent generale (Opus) su 6 controller monitoring non ancora verificati (covenant/document/genai/spread/mailing-list/covno) ha trovato 8 bug pre-runtime. Fix in batch (commit BE `0df3952`):
    - **Practice findAll**: sort camelCase `practiceCode` non valido in native query (`Unknown column 'v.practiceCode'`). Aggiunto `SORT_COLUMN_MAP` camelCase→snake_case in `MonitoringPracticeService.createPageable` con whitelist esplicita (no SQL injection). Filtro `is_archived='NO'` rimosso (ENUM DB e' `'ARCHIVED'/'NOT_ARCHIVED'`, M1 ha NULL → 0 risultati sempre; la view stessa gia' filtra archived+draft). Filtro `semaphoreStates` aggiunto prefix `MONITORING_` (FE manda `OK`/`IN_SCADENZA`, DB ha `MONITORING_OK`).
    - **Practice export Excel**: BE aveva `@GetMapping("/export")` con `@RequestParam`, FE chiama `POST` con body. Cambiato a `@PostMapping + @RequestBody MonitoringPracticeFilterDTO`.
    - **MonitoringCovenantDTO mancava 5 campi**: `state`, `countEvents`, `countExpired`, `countExpiring`, `countOk` consumati dal FE per la tabella covenant del dettaglio pratica. Aggiunti al DTO e popolati in `toDto` via stream su `MonitoringEvent` del covenant + `monitoringStateService.calculateCovenantState` (riusa logica B3 BR).
    - **Dashboard /expired e /expiring ignoravano i filtri FE** (`practiceCode`/`trackingId`/`dealName`/`covenantType`/`expirationDateFrom/To`/`sortBy/Direction`): solo `page+size` venivano applicati. Refactor: nuovo `MonitoringDashboardEventFilterDTO`, `MonitoringDashboardService` con `collectEventsByStates` + `applyFilters` (6 filtri stream) + `sortInMemory` (8 campi DTO) + `paginateAndSort`. Controller espone nuovi `@RequestParam` con `LocalDate` per le date.
    - **Dashboard events `status`** popolato con `.getText()` (label IT "In Scadenza") invece di `.name()` (`IN_SCADENZA`). FE `SemaphoreComponent` matcha solo i code enum → tutti gli altri stati restavano grigio default. Cambiato a `.name()`.
    - **Dashboard export Excel mancava** 2 endpoint (`/dashboard/expired/export`, `/dashboard/expiring/export`): FE 404 al click "Esporta". Aggiunti i 2 endpoint + nuovo `MonitoringDashboardEventExcelWriterService` (XLSX 13 colonne, pattern `GenericExcel`/`GenericWriterSheet`).
    - **Bug declassato** (non scatenabile da smoke): `getExceptionDocument` FE expecta `Blob` ma BE manda `List<MonitoringDocumentDTO>` JSON. Il metodo FE non e' cablato in nessun componente e lo storage del file in `requestException` salva solo metadati. Da affrontare in T-024 quando il button "Visualizza Eccezione" verra' cablato.
  - **3 bug residui STEP 4 emersi a runtime** dopo rebuild BE (commit BE `b2332de` + FE `573f1d5`):
    - Filtro Tracking ID nella tabella tornava 0 risultati: la query nativa usava `v.deal_id = :trackingId` (equality) invece di `LIKE CONCAT('%', :trackingId, '%')`. Allineato al pattern degli altri 3 filtri stringa.
    - Filtro multi semaforo (es. OK + In Scadenza assieme) generava `SQL Error 1241 — Operand should contain 1 column(s)`: Hibernate sostituiva la tupla `(?,?) IS NULL` non valida in MySQL. Risolto rimuovendo il NULL check e passando sempre una lista non-vuota (default: tutti i 6 stati MONITORING_* se l'utente non filtra). Con un solo elemento funzionava perche' la tupla a 1 elemento non veniva generata.
    - **Sfalsatura colonne tabella**: le tabelle `practices` e `dashboard.recent` mostravano `practiceId` (numerico 8288) sotto la colonna "ID" e tutte le altre colonne erano shiftate di 1 posizione (Tracking ID mostrava il code M1, ecc.). Causa: il template generico di `app-table` itera su `Object.keys(row)` in ordine di insertion e mappa la cell `i` sull'header `thead[i]`; il row aveva 14 properties (practiceId + 13 visibili) mentre `thead` ne aveva 13. Fix: aggiunta in cima a entrambi i `thead` una entry hidden `{ field: 'practiceId', hidden: true }`. Mantiene la disponibilita' di `row['practiceId']` per `onRowClick`/`onTerminatePractice`.
  - **Stato STEP smoke**: 1-3 DONE. 4 IN CORSO (filtri base + export verificati, tabella allineata; mancano test filtri data e ordinamento colonne post-rebuild). 5-10 PENDING. M1 mantenuta in DB (non terminata, serve per STEP 5+).
  - **Pushed**: BE `feature/monitoring-integration-uat` (a5ed8ff..b2332de — 3 commit: 888e704, 0df3952, b2332de), FE `feature/monitoring-integration-uat` (b396515..573f1d5 — 2 commit: bdc195a, 573f1d5).
  - **Bug noti residui per STEP 5+**: (a) `mp.deal_id`+`mp.contract_key` valorizzati NULL su M1 (e su pratica padre 8265). Il `createFromPostClosing` non copia `deal_id`/`contract_key` dalla pratica padre — da verificare se intenzionale o gap T-024. (b) `MailingListAdForm.state` mai persistito in `VERIFICA_IN_CORSO` (overwritten subito a `REPORT_GENERATO`) — gap UX vs spec. (c) `MonitoringPracticeExcelWriterService` ha stesso bug `Enum::name` senza prefix MONITORING_ sui semaphoreStates filter — da fixare se l'export viene chiamato con filtro semaforo non-vuoto.
- **STEP 5 Dettaglio Pratica chiuso (Davide)**: la pagina `/user/monitoring/practice/8288` carica correttamente con M1 vuota (0 covenant/event/document). Header mostra `practiceCode`/`mandateType`/`bankUserName`/`semaphoreState`. 3 tab visibili (Covenant/Spread/Scadenziere). Console pulita, no 500. Nessun bug a runtime — i fix STEP 4 hanno coperto anche i contratti del dettaglio (`MonitoringPracticeSummaryDTO.semaphoreState`, `MonitoringCovenantDTO` con counts).
- **STEP 5b UI "Aggiungi Covenant" implementata (Davide)**: il FE non aveva UI per creare covenant (gap noto T-024 dal PLAN riga 116). Senza covenant non si possono testare gli STEP 6-10 (eccezioni, GenAI, COVNO, mailing, expired/expiring). Implementata UI minimale a copertura **BR sez. 4.2.5 step 1** (Modifica Dati Covenant — il flow completo "Aggiungi Monitoraggio" sui covenant esistenti resta gap T-024).
  - **Componenti aggiunti** (commit FE `453346c`):
    - `MonitoringCovenantAddDialogComponent` (`practice-detail/covenant-add-dialog/`): dialog standalone con form Reactive (tipologia/segno/valore limite/periodicita'/variabile/note); chiama `monitoringService.createCovenant(practiceId, payload)`; on success emette `covenantCreated` → parent richiama `loadData()`.
    - 3 enum FE: `CovenantSignEnum` (5 valori + `COVENANT_SIGN_SYMBOL` mapping a `>`/`>=`/`=`/`<`/`<=`), `MonitoringPeriodicityEnum` (5 valori), `CovenantTypeNumericEnum` (subset 10 tipologie su 58 BE — la lista completa va migrata a endpoint API dedicato per evitare drift, segnato come followup T-024.x).
    - `MonitoringCovenantAddDialogModule`: ReactiveFormsModule + Dropdown + InputNumber + InputText + InputTextarea + Dialog + Button + Pipe.
  - **Wiring**: button "Aggiungi Covenant" `pi-plus` accanto all'header "Overview Covenant" gated `*appHasRole=Consultant` (solo Deloitte); `@ViewChild('covenantAddDialog')` + `openAddCovenant()` + `onCovenantCreated()` in `practice-detail.component.ts`; `<app-monitoring-covenant-add-dialog>` mounted in fondo al template accanto al exception dialog; import del nuovo module in `practice-detail.module.ts`.
  - **Service FE**: nuovo metodo `createCovenant(practiceId, payload)` in `monitoring.service.ts` che POST a `/monitoring/practices/{id}/covenants` (endpoint BE gia' implementato).
  - **i18n**: aggiunte chiavi IT+EN `monitoring.covenant.add.*` (button/title/labels form) + `monitoring.covenantType.*` (10 traduzioni) + `monitoring.periodicity.*` (5 traduzioni) + `monitoring.actions.save`.
  - **Lato BE**: nessuna modifica. L'endpoint `POST /monitoring/practices/{practiceId}/covenants` era gia' implementato + `MonitoringCovenantService.create()` chiama `scheduleGeneratorService.generateAndSaveEvents(saved)` che auto-genera N eventi del covenant in base a periodicita' + `termination_date` della pratica padre.
  - **Verifiche**: `npx tsc --noEmit --skipLibCheck` PASS, JSON i18n IT+EN validi.
  - **Limitazioni note**: (i) CovenantType limitato a 10 tipologie hardcoded — full list da spostare in endpoint BE che ritorna `Map<String, String>` o `List<TypologicalDTO>`. (ii) Manca il flow "Modifica Dati Covenant" + "Aggiungi Monitoraggio" + "Rimuovi Monitoraggio" descritti in BR sez. 4.2.5 — il bottone "Aggiungi Covenant" implementato e' una semplificazione pragmatica per UAT smoke. La spec BR prevede la modifica del singolo covenant (entrare nel dettaglio covenant → "Modifica Dati Covenant" → form editabile inline) come strato successivo, da implementare in iterazione T-024.x.
- **STEP 5b post-creazione bug fix (Davide)**: dopo aver creato M1C1 via UI sono emersi 4 bug. Tutti risolti.
  - **(1) Generator eventi 0 risultati** (commit BE `1c91c66`): `MonitoringScheduleGeneratorService.generateEvents` partiva da `LocalDate.now()` (2026-05-25) ma la `termination_date` di M1 era `2026-05-14` (passata, fixture POST_CLOSING vecchia) → `while (!startDate.isAfter(terminationDate))` non entra mai → 0 eventi salvati nel DB. Il bug si manifesta ogni volta che la pratica monitoring viene creata DOPO la termination_date del deal padre (caso reale se POST_CLOSING chiuso tardi rispetto a deadline contrattuale o pratica riaperta). Fix logic: nuova helper `resolveStartDate()` che usa `practice.creationDate.toLocalDate()` come startDate con fallback `LocalDate.now()` se null. Coerente con BR sez. 3.1 ("la genesi e' dal momento di creazione della pratica monitoring"). Fix DB locale: `UPDATE mp_practice SET termination_date='2027-12-31' WHERE id=8288` + cancellazione M1C1 esistente per retest pulito via UI.
  - **(2) Button "Dettaglio Covenant" non cliccabile** (commit FE `df82634`): la colonna "Azione" della tabella "Tutti i Covenant" mostrava solo il testo statico "Dettaglio Covenant" invece di un button. Fix: `mapCovenantToRow` ora ritorna `DynamicComponentModel<ButtonComponent>` con icona `pi-search`, click$ → `openCovenantDetail(covenant)`. Aggiunti `activeCovenant`/`covenantDetailVisible` + handler `openCovenantDetail`/`closeCovenantDetail` + wired `<app-covenant-detail>` in fondo al template con `*ngIf="activeCovenant"`.
  - **(3) Label i18n mancanti** (stesso commit FE `df82634`): `monitoring.documents.history.title` (h4 storico documenti) + `monitoring.documents.actions` (header colonna). Quest'ultima aveva collision strutturale impossibile: usata sia come stringa header sia come oggetto per i tooltip `actions.download/confirm/reject`. Fix: aggiunte chiavi `monitoring.documents.history.title` + `monitoring.documents.columnAction` (header, nuova chiave) + `monitoring.documents.actions.{download,confirm,reject}` (oggetto tooltip) IT+EN. Modificato `document-history.component.ts:179` per usare `columnAction` invece di `actions`.
  - **(4) Dialog form vs mockup**: il dialog "Aggiungi Covenant" implementato e' una **deviazione consapevole dal mockup BR**. Il BR sez. 4.2.5 + mockup pagine 26-30 prevedono una PAGINA full-page "Dettaglio Pratica X | Covenant Y" con header dati pratica, sezione "Status covenant" con avanzamento, tabella scadenziere editabile inline (modify/aggiungi/rimuovi monitoraggio direttamente in row), pulsante "Modifica Dati Covenant" che switcha la pagina in modalita' modifica con bottoni Salva/Annulla/Aggiungi Monitoraggio. Il dialog modale implementato copre SOLO il caso "creazione nuovo covenant from-scratch" (gap T-024.x) che non e' nemmeno esplicitamente nel BR — assunzione: i covenant nascono automaticamente da POST_CLOSING+GenAI. Da migrare a pagina dedicata in iterazione successiva (T-024.x: Covenant Detail Edit Page).
  - **Pushed**: BE `feature/monitoring-integration-uat` (b2332de..1c91c66 — 1 commit), FE `feature/monitoring-integration-uat` (453346c..df82634 — 1 commit).
- **STEP 5c Modifica Dati Covenant full-page + fix residui covenant-detail (Davide)**: implementata pagina dedicata BR sez. 4.2.5 + risolti 3 bug residui sul dialog covenant-detail emersi nello smoke. Durata ~2h.
  - **BE — 3 endpoint CRUD event** (commit `25ba16f`):
    - `POST /monitoring/covenants/{covenantId}/events` con `referenceDate` + `expirationDate` query params → `addEventToCovenant` nel service. Genera `eventCode` incrementale `covenantCode + E + (size+1)`, stato NESSUNO_STATO iniziale, hasException=false. Ricalcola lo stato dell'event via `calculateEventState` (puo' diventare SCADUTO/IN_SCADENZA se le date sono passate/imminenti) + propaga via `recalculateAllStates`.
    - `PUT /monitoring/events/{eventId}` con `referenceDate` + `expirationDate` query params → riutilizza il metodo `updateDates` gia' esistente nel service (originariamente per il flusso GenAI T-018) per la modifica inline manuale Deloitte.
    - `DELETE /monitoring/events/{eventId}` → `deleteEvent` transactional + recalculateAllStates.
    - Iniezione `MonitoringCovenantRepository` nel `MonitoringEventService` (necessaria per leggere il covenant in addEventToCovenant).
  - **FE — Pagina Modifica Dati Covenant** (commit `c3a90ac`):
    - Nuovo componente `MonitoringCovenantEditPageComponent` + html + scss. Route lazy `covenant/:covenantId/edit` aggiunta nel `practice-detail.module` routes → path assoluto `/{role}/monitoring/practice/:id/covenant/:covenantId/edit`.
    - Layout coerente con mockup BR riga 1250+: header dati pratica (riusa stesso layout di practice-detail) + toolbar "Torna alla pratica" a sinistra, button "Modifica Dati Covenant" oppure trio Salva/Annulla/Aggiungi Monitoraggio a destra (switch via flag `editMode`) + sezione "Status Covenant" con form Reactive (Tipologia dropdown + Segno dropdown + Valore Limite inputNumber + Periodicita' dropdown + Variabile input + Note textarea) tutti disabled in view mode, enabled in edit mode + tabella scadenziere (`p-table`) con inline-edit per `referenceDate` e `expirationDate` (input type=date) + pulsante trash per Rimuovi Monitoraggio (segna `toDelete=true`, row strike-through) con pulsante undo per annullare; per le righe non ancora salvate (isNew=true) rimozione immediata. "Aggiungi Monitoraggio" push nuova row vuota con isNew=true editabile subito.
    - Save: `forkJoin` di N chiamate (PUT covenant se dirty + N POST/PUT/DELETE event a seconda della riga). Su success → `openSuccessDialog` + exit edit mode + reload dati. Cancel: ripristina i valori iniziali + ricostruisce le rows dagli events caricati.
  - **FE — Fix 3 bug covenant-detail dialog** (stesso commit `c3a90ac`):
    1. **"Salta Monitoraggio" / "Ignora Soglia" / "Visualizza Eccezione" non erano button**: `mapEventToRow` ritornava `action: actionText` come stringa statica. Sostituito con `buildActionCell()` che ritorna un vero `ButtonComponent` role-gated: Visualizza Eccezione (sempre se hasException), Salta Monitoraggio (ISP only, se daysToNextMonitoring<0 + no actualDate), Ignora Soglia (ISP only, se status SCADUTO/FUORI_SOGLIA), `'—'` altrimenti. Click$ emette `onRequestException` / `onViewException` al parent che apre l'exception dialog gia' esistente.
    2. **Semaforo grigio per eventi futuri NESSUNO_STATO**: il casting `event.status as MonitoringPracticeStateEnum` produceva semaforo grigio fallback (lo stateColorMap non aveva la chiave). Sostituito con check esplicito: se `status === NESSUNO_STATO || !status` ritorna `'—'`, altrimenti SemaphoreComponent. Coerente col mockup BR riga 1339+ (eventi futuri lontani mostrano "—" non pallino). Stesso fix applicato anche in `practice-detail.component.mapEventToRow` per gli accordion Scaduti/In Scadenza.
    3. **Toolbar dialog**: aggiunto button "Modifica Dati Covenant" gated Deloitte che emette `onEditCovenant` al parent. Parent `practice-detail` ha `navigateToEditCovenant(covenant)` che chiude il dialog e naviga alla nuova pagina.
  - **FE — Service / Rest**:
    - `MonitoringService` esteso con `updateCovenant`/`deleteCovenant`/`addEvent`/`updateEventDates`/`deleteEvent`.
    - `RestService` esteso con parametro opzionale `params?: any` per `post`/`put`/`patch`/`delete` (le 3 chiamate event usano query params anziche' body).
  - **i18n IT+EN**: 10 chiavi nuove `monitoring.covenant.editPage.*` (covenantId/statusCovenant/type/limitValue/variable/editButton/addMonitoring/removeMonitoring/undoRemove/newEventCode/noEvents).
  - **Limiti noti**:
    - Campo "Variabile" e' input text libero invece di dropdown SI/NO come da BR sez. 4.2.5 — il dominio BE espone `variable` come String generica. `variableOptions` dichiarato nel TS ma non usato nel template (placeholder).
    - PUT covenant aggiorna solo i metadati; se cambia la `periodicity` il numero/posizione degli eventi NON viene rigenerato automaticamente. Fix futuro: chiamare `scheduleGeneratorService.regenerateEvents()` quando `periodicity` cambia.
  - **Verifiche**: BE `mvn compile` SUCCESS. FE `tsc --noEmit --skipLibCheck` PASS. JSON i18n IT+EN validi.
  - **Pushed**: BE `feature/monitoring-integration-uat` (1c91c66..25ba16f — 1 commit), FE `feature/monitoring-integration-uat` (df82634..c3a90ac — 1 commit).
